import React from 'react';
import './ButtonProps.css'

const ButtonProps = () => {
    return (
        <button className='button'>
            Click2
        </button>
    );
};

export default ButtonProps;